{
  "name": "BR-Cockpit",
  "dockerComposeFile": "docker-compose.yml",
  "service": "entwicklung",
  "workspaceFolder": "/workspace",

  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },

  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": {
      "label": "BR-Cockpit",
      "onAutoForward": "openBrowser"
    }
  },

  // Abhaengigkeiten, Schema und Beispielbestand in einem Rutsch.
  "postCreateCommand": "bash .devcontainer/einrichten.sh",

  // Ohne diese Zeile gibt Codespaces das Terminal frei, waehrend
  // postCreateCommand noch laeuft. Da "npm ci" das Verzeichnis node_modules
  // zuerst loescht, liefe ein sofortiges "npm run dev" in "next: not found".
  "waitFor": "postCreateCommand",

  "customizations": {
    "vscode": {
      "extensions": [
        "Prisma.prisma",
        "bradlc.vscode-tailwindcss",
        "dbaeumer.vscode-eslint"
      ]
    }
  },

  "remoteUser": "node"
}
